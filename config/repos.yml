# Feather 项目配置文件

repositories:
  - name: Kazumi
    owner: Predidit
    repo: Kazumi
    json_file: app/kazumi.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: PiliPlus
    owner: bggRGjQaUbCoE
    repo: PiliPlus
    json_file: app/piliplus.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: Fluxdo
    owner: Lingyan000
    repo: fluxdo
    json_file: app/fluxdo.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: Harbour
    owner: rrroyal
    repo: Harbour
    json_file: app/harbour.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: PeekPili
    owner: ingriddaleusag-dotcom
    repo: PeekPiliRelease
    json_file: app/peekpili.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: Asspp
    owner: Lakr233
    repo: Asspp
    json_file: app/asspp.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

  - name: Conduit
    owner: cogwheel0
    repo: conduit
    json_file: app/conduit.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "16.0"

  - name: YueYin
    owner: xiaoqi419
    repo: joy-music-mobile
    json_file: app/yueyin.json
    ipa_filename_pattern: "*.ipa"
    min_os_version: "13.0"

paths:
  app_dir: app
  all_json: all.json

logging:
  level: INFO
  format: text

merge:
  # app/*.json 为唯一数据源，合并时删除 all.json 中的孤儿应用
  remove_orphans: true

update:
  max_workers: 4
  max_retries: 3
