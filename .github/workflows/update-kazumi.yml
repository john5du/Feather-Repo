name: Update Kazumi Repo JSON from Kazumi Releases

on:
  schedule:
    - cron: '0 */23 * * *'  # 每12小时运行一次
  workflow_dispatch:  # 允许手动触发

jobs:
  check-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests PyGithub

      - name: Check releases and update kazumi.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cat > update_kazumi.py << 'EOF'
          import json
          import os
          from datetime import datetime
          from github import Github
          import requests

          def get_file_size(url):
              response = requests.head(url)
              return int(response.headers.get('content-length', 0))

          def main():
              # 初始化GitHub客户端
              g = Github(os.environ['GITHUB_TOKEN'])
              repo = g.get_repo("Predidit/Kazumi")
              
              # 获取最新release
              latest_release = repo.get_latest_release()
              
              # 查找IPA文件
              ipa_asset = None
              for asset in latest_release.get_assets():
                  if asset.name.endswith('.ipa'):
                      ipa_asset = asset
                      break
              
              if not ipa_asset:
                  print("No IPA file found in latest release")
                  return
              
              # 读取现有的kazumi.json
              try:
                  with open('kazumi.json', 'r', encoding='utf-8') as f:
                      source_data = json.load(f)
              except FileNotFoundError:
                  print("kazumi.json not found")
                  return
              
              # 更新版本信息
              source_data['apps'][0].update({
                  'version': latest_release.tag_name.lstrip('v'),
                  'versionDate': latest_release.published_at.strftime("%Y-%m-%dT%H:%M:%SZ"),
                  'downloadURL': ipa_asset.browser_download_url,
                  'size': ipa_asset.size,
                  'versionDescription': latest_release.body
              })
              
              # 保存更新后的文件
              with open('kazumi.json', 'w', encoding='utf-8') as f:
                  json.dump(source_data, f, ensure_ascii=False, indent=2)

          if __name__ == '__main__':
              main()
          EOF
          
          python update_kazumi.py

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add kazumi.json
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update kazumi.json with latest release info [skip ci]"

      - name: Push changes
        run: git push
